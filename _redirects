# Cloudflare Pages manda un 308 de /loquesea.html a /loquesea. Para las páginas
# del sitio está bien —hay una sola URL buena y el sitemap usa esa—, pero a
# Google Search Console le estorba: pide su fichero de verificación en la URL
# exacta que él da, con el .html puesto, y su documentación dice que no se
# redirija.
#
# Un 200 es una REESCRITURA, no una redirección: la URL se queda con su .html y
# Cloudflare sirve por debajo el fichero. Se apunta a la versión sin extensión
# —que es la que Pages sirve de verdad— y no a sí mismo, que haría bucle.
/google279fb54ea943369d.html  /google279fb54ea943369d  200
